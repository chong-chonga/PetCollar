本项目代码规定: 类的开始/结尾大括号 与代码的开始/结尾 相隔一行
         静态字段位于类的最上层, 往下依次是属性字段
         静态字段之间间隔为1行, 属性字段之间间隔1行
         相关的两个方法之间间隔2行, 无关的两个方法间隔3行
         方法的排列顺序应当是根据调用规则排列, 自上而下排列
         单个方法的代码段尽量不要出现重复, 且抽象层级要一致, 名称应当尽量与代码所做的事情一致
         长而易懂的名称比短而有歧义的名称的更好
pojo类规范: 只允许有对应表的属性字段, 除常规取值/赋值器之外, 不允许提供对外接口方法
格式缩进: 修饰符占一行, 类型符以及字段名称占一行, 相邻两个属性字段之间间隔1行

有返回值的方法, 如果要返回 null, 可以考虑抛出异常

业务方法的参数顺序应当为: 权限令牌, 资源名称, 请求参数
超过三个参数的请求方法, 应当封装为一个类
字段值为null时不做返回
对于添加（POST）、修改（PUT | PATCH 返回必要的数据
